extends layout

block content
    h1= title

    ol
        each book in book_list
            li
                a(href=book.url) #{book.title}
                |   (#{book.author.family_name}, #{book.author.first_name})

        else
            b There are no books